[pytest]
addopts = --durations=10
markers = 
    prelaunch: marca para tests que se ejecutan como parte de la validación de pre-lanzamiento.
    slow: marca para tests que tardan mucho en ejecutarse.
    integration: marca para tests de integración que pueden requerir servicios externos. 