diff --git a/src/scripts/bolsa_service.py b/src/scripts/bolsa_service.py
index e8f4ab1..######## 100644
--- a/src/scripts/bolsa_service.py
+++ b/src/scripts/bolsa_service.py
@@
-from .browser_refresh import (
-    find_chromium_process,
-    refresh_chromium_tab,
-)
+from utils.browser_refresh import (
+    find_chromium_process,
+    refresh_chromium_tab,
+)
+
+# Funciones trasladadas a utils ----------------------------------------------
+from utils.db_io import (
+    _build_price_summary,
+    store_prices_in_db,
+    get_latest_data,
+    compare_last_two_db_entries,
+)
+
+from utils.scheduler import (
+    update_data_periodically,
+    start_periodic_updates,
+    stop_periodic_updates,
+)
 
@@
-# --------------  Copia SIN CAMBIOS cada función a partir de aquí  --------------
-                                                                       ^
-                                                                       |-----------
-                   TODO: este bloque entero (funciones JSON, DB, scheduler)
-                         ya está en utils — elimínalo para aligerar el archivo.
-                                                                       |-----------
-                                                                       v
-
-def get_latest_json_file():
-    ...
-
-def extract_timestamp_from_filename(filename: str) -> str:
-    ...
-
-def get_json_hash_and_timestamp(path: str):
-    ...
-
-def store_prices_in_db(json_path: str, app=None):
-    ...
-
-def get_latest_data():
-    ...
-
-def compare_last_two_db_entries():
-    ...
-
-def update_data_periodically(min_interval_s: int, max_interval_s: int, app=None):
-    ...
-
-def start_periodic_updates(min_minutes: int = 15, max_minutes: int = 45, app=None) -> bool:
-    ...
-
-def stop_periodic_updates() -> bool:
-    ...
-
-# --------------  Fin del bloque duplicado ------------------------------------
+# --------------  Fin del bloque eliminado  -----------------------------------
