diff --git a/src/scripts/bolsa_service.py b/src/utils/scheduler.py
copy from src/scripts/bolsa_service.py
copy to src/utils/scheduler.py
@@
-update_thread: Optional[threading.Thread] = None
-stop_update_thread = False
-
-def update_data_periodically(min_interval_s: int, max_interval_s: int, app=None):
-    ...
-
-def start_periodic_updates(min_minutes: int = 15, max_minutes: int = 45, app=None) -> bool:
-    ...
-
-def stop_periodic_updates() -> bool:
-    ...
+# módulo trasladado: utils/scheduler.py
+from __future__ import annotations
+
+import logging, random, threading, time
+from typing import Optional
+
+from src.scripts.bolsa_service import run_bolsa_bot, is_bot_running  # import ligero
+
+logger = logging.getLogger(__name__)
+
+update_thread: Optional[threading.Thread] = None
+stop_update_thread = False
+
+def update_data_periodically(min_interval_s: int, max_interval_s: int, app=None):
+    ...
+
+def start_periodic_updates(min_minutes: int = 15, max_minutes: int = 45, app=None) -> bool:
+    ...
+
+def stop_periodic_updates() -> bool:
+    ...
+
+__all__ = [
+    "update_data_periodically",
+    "start_periodic_updates",
+    "stop_periodic_updates",
+]
